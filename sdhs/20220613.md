## 2022.06.13(월)

### 지금은 회사에서 컴퓨터 세팅 중에 비쥬얼 코드를 설치중에 쓴다
### 디지텍숲 mvc만들다 말았음
```c
    static void Main(string[] args)
       {
            Program pr = new Program();
            //pr.sum();
            pr.gcmLcmFindFn(10, 15);
        }
        // 구구단 메소드
        public void sum()
        {
               for (int i = 1; i <= 9; i++)
               {
                   Console.WriteLine(i + "단");
                   for (int y = 1; y <= 9; y++) {
                        Console.WriteLine(i + "x" + y +"="+ i*y);
                   }
               }
        }
        // 두 수를 입력 받아 최대공약수, 최소공배수 구하기
        public void gcmLcmFindFn(int firstNum, int lastNum)
        {
            Boolean cont = true;
            for (int i = 1; cont; i++)
            {
                if (firstNum / i == 0 && lastNum / i == 0)
                {
                    cont = false;
                    Console.WriteLine(i);
                }
            }
        }
        // 피보나치 수열
                public void fibonacciSequence()
        {
            int sumNum = 0;
            for (int i = 1; i < 50; i++)
            {
                sumNum = i;
                if (i % 3 == 0)
                {
                    i = i + sumNum;
                    Console.Write(i);
                    
                }
                else
                {
                    Console.Write(i);
                }
               
            }
        }

        // 다이아몬드 찍기 메소드
        //        *  
        //       ***
        //      *****
        //     *******
        //    *********
        //     *******
        //      *****
        //       ***
        //        *
        //
        //    star = star == 0 ? 1  : line * 2 + 1;
        //    line for문 돌때마다 ++
        //    space 
        //    개씩 증가
        //    space = star 
        public void diamond()
        {
            int space = 0;
            int stat = 0;
            int lineCnt = 0;
            for (int line = 0; line < lineCnt; line++) { 
            
             }
            //for()
        }
        

```
